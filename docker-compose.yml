version: '3.8'

services:
  app:
    build: .
    container_name: auto_prog
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    networks:
      - giv_network

networks:
  giv_network:
    external: true
